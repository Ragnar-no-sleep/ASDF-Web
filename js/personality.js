import ASDF_ENDPOINTS from "./config/endpoints.js";
const ARCHETYPES = { philosophe: { name: "Le Philosophe", tagline: "Tu joues où les autres ne regardent pas encore.", stats: [{ label: "Réflexion", value: 85, displayValue: "Profonde" }, { label: "Patience", value: 92, displayValue: "Infatigable" }, { label: "Prise de risque", value: 15, displayValue: "Nulle" }] } };
class PersonalityEngine { constructor() { this.board = document.querySelector(".board-grid"); this.gameView = document.getElementById("game-view"); this.resultView = document.getElementById("result-view"); this.init(); }
init() { this.renderBoard(); const boardEl = document.getElementById("chess-board"); if (boardEl) boardEl.addEventListener("click", () => this.fetchIdentity()); }
renderBoard() { if (!this.board) return; for (let i = 0; i < 16; i++) { const cell = document.createElement("div"); const isLight = (Math.floor(i / 4) + i) % 2 === 0; cell.className = "cell " + (isLight ? "light" : "dark"); if (i === 5) cell.innerHTML = "<span class=\"piece\">?</span>"; if (i === 10) cell.innerHTML = "<span class=\"piece\">?</span>"; this.board.appendChild(cell); } }
async fetchIdentity() { const boardHint = document.querySelector(".board-hint"); if (boardHint) { boardHint.textContent = "Analyse de ton âme..."; boardHint.style.opacity = "1"; } try { const response = await fetch(ASDF_ENDPOINTS.api + "/personality"); if (!response.ok) throw new Error("API offline"); const data = await response.json(); this.renderResult(data); } catch (e) { this.renderResult({ archetype: ARCHETYPES.philosophe, confidence: 0.9, stats: ARCHETYPES.philosophe.stats }); } }
renderResult(data) { if (!this.gameView || !this.resultView) return; this.gameView.classList.remove("active"); this.resultView.classList.add("active"); const confidenceLabel = data.confidence > 0.8 ? "Révélé sur 12 parties" : "Révélé sur 3 parties"; let statsHtml = data.stats.map(s => "<div class=\"stat-item\"><div class=\"stat-info\"><span>" + s.label + "</span><span>" + (s.displayValue || s.value) + "</span></div><div class=\"stat-bar-container\"><div class=\"stat-bar-fill\" style=\"width: " + s.value + "%\"></div></div></div>").join(""); this.resultView.innerHTML = "<div class=\"personality-card\"><div class=\"decorative-line\"></div><div class=\"card-content\"><header><h2 class=\"archetype-name\">" + data.archetype.name + "</h2><p class=\"archetype-tagline\">\"" + data.archetype.tagline + "\"</p></header><div class=\"stats-section\">" + statsHtml + "</div><footer class=\"card-footer\"><span class=\"confidence-text\">" + confidenceLabel + "</span><div class=\"brand-signature\">Blitz & Chill</div></footer></div></div><button onclick=\"location.reload()\" style=\"margin-top: 40px; background: transparent; border: 1px solid var(--forest-700); color: var(--amber-500); font-family: Fraunces, serif; padding: 10px 30px; cursor: pointer;\">Retour au calme</button>"; } }
new PersonalityEngine();
