# ===========================================
# ASDF API Configuration
# ===========================================
# Copy this file to .env and fill in your values
# NEVER commit the .env file to version control!

# Server Configuration
API_PORT=3001
NODE_ENV=development

# ===========================================
# HELIUS RPC (Required)
# ===========================================
# Get your API key at https://helius.xyz
# Free tier: 100k credits/month
HELIUS_API_KEY=your_helius_api_key_here
HELIUS_RPC_URL=https://mainnet.helius-rpc.com/?api-key=your_helius_api_key_here

# Optional: Backup RPC for failover (recommended for production)
# Can be another Helius key or a different provider
# BACKUP_RPC_URL=https://api.mainnet-beta.solana.com

# ===========================================
# SOLANA TOKEN CONFIGURATION
# ===========================================
# ASDF Token Mint Address (mainnet)
ASDF_TOKEN_MINT=9zB5wRarXMj86MymwLumSKA1Dx35zPqqKfcZtK1Spump

# Treasury wallet for SOL payments (if needed)
TREASURY_WALLET=your_treasury_wallet_here

# ===========================================
# AUTHENTICATION (Required)
# ===========================================
# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# MUST be at least 32 characters!
JWT_SECRET=generate_a_strong_random_secret_at_least_32_chars

# ===========================================
# CORS CONFIGURATION
# ===========================================
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,https://alonisthe.dev

# ===========================================
# POSTGRESQL DATABASE
# ===========================================
# Create a PostgreSQL database on Render:
# 1. Render Dashboard > New > PostgreSQL
# 2. Copy "Internal Database URL" for same-region
# 3. Set DATABASE_URL below

# Connection string format:
# postgresql://user:password@host:port/database
# DATABASE_URL=postgresql://asdf_user:xxx@dpg-xxx.frankfurt-postgres.render.com/asdf_db

# For Render internal connection (faster, same region):
# DATABASE_URL=postgres://asdf_user:xxx@dpg-xxx/asdf_db

# ===========================================
# STORAGE BACKEND
# ===========================================
# Options: 'memory' (default) or 'redis'
STORAGE_BACKEND=memory

# ===========================================
# REDIS CONFIGURATION
# ===========================================
# Set STORAGE_BACKEND=redis to enable

# Option 1: Connection URL (recommended for cloud services)
# Format: redis[s]://[[username][:password]@][host][:port][/db-number]
# REDIS_URL=redis://localhost:6379
# REDIS_URL=rediss://default:password@hostname:6379  # TLS connection

# Option 2: Individual settings (alternative to URL)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=your_redis_password
# REDIS_DB=0

# Key prefix (namespacing)
REDIS_PREFIX=asdf:

# TLS for cloud Redis (Render, Railway, etc.)
# REDIS_TLS=true

# Timeouts (in milliseconds)
# REDIS_CONNECT_TIMEOUT=8000
# REDIS_COMMAND_TIMEOUT=5000

# ===========================================
# RENDER.COM REDIS CONFIGURATION
# ===========================================
# When using Render Redis:
# 1. Create a Redis instance in your Render dashboard
# 2. Copy the "Internal Redis URL" for same-region services
# 3. Set: REDIS_URL=<your-internal-redis-url>
# 4. Set: STORAGE_BACKEND=redis
# 5. TLS is automatic with Render internal URLs

# ===========================================
# UPSTASH REDIS CONFIGURATION
# ===========================================
# When using Upstash (serverless Redis):
# 1. Create a database at https://upstash.com
# 2. Copy the Redis REST URL
# 3. Set: REDIS_URL=rediss://default:<password>@<endpoint>:6379
# 4. Set: STORAGE_BACKEND=redis
# Note: Upstash uses TLS by default (rediss://)

# ===========================================
# OPTIONAL: Webhook Configuration
# ===========================================
# For Helius webhooks (real-time burn tracking)
# HELIUS_WEBHOOK_SECRET=your_webhook_secret_here

# ===========================================
# OPTIONAL: Push Notifications
# ===========================================
# Required if using push notifications
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# PUSH_TOKEN_SECRET=your_push_token_secret_here
# VAPID_PRIVATE_KEY=your_vapid_private_key
# VAPID_PUBLIC_KEY=your_vapid_public_key
